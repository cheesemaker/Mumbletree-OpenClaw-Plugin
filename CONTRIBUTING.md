# Contributing

Contributions are welcome through pull requests.

Before submitting:

```sh
npm ci
npm run verify
```

Use synthetic identifiers and messages in tests. Never commit production bot
credentials, pairing codes, private messages, real channel IDs, OpenClaw
configuration files, or host-specific deployment state.
